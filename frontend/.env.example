# Frontend environment variables
# For local dev: Vite proxy handles /api → localhost:8000 (no VITE_API_URL needed)
# For Vercel / production: set this to your actual backend URL

# VITE_API_URL=https://api.yourdomain.com/api/v1
